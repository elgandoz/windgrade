# Windgrade

Instructions live in AGENTS.md so every agent reads the same file.

@AGENTS.md

Also read, when relevant:

- `docs/plan.md` — the phased plan and the decisions still open
- `docs/handover.md` — full background: why each architectural choice was made
- `docs/findings.md` — probe results; empty until `probe.html` has been run
